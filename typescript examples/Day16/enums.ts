enum colors {red,green,blue,balck};

enum direc {
    left,right,up,down
};

enum Gender {M,F};

let c : colors;
c = colors.balck;
document.write(`<p>The color is ${c}</p>`);

let d : direc;
d = direc.left;
document.write(`<P>The direction is ${d}</p>`);

let g : Gender;
g = Gender.M;
document.write(`<p>U r a ${g}</p>`);